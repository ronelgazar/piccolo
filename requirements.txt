# Piccolo – Stereoscopic Surgery Display
opencv-python>=4.8
numpy>=1.24
pyyaml>=6.0
screeninfo>=0.8
Pillow>=10.0
PyQt6>=6.6
PyTurboJPEG>=1.7
PyOpenGL>=3.1
PyOpenGL_accelerate>=3.1

# --- Raspberry Pi only (uncomment when deploying on RPi) ---
# picamera2>=0.3

pytest>=8.0       # dev only — test runner
pytest-qt>=4.3    # dev only — Qt widget smoke tests
